name: 编译 京东云雅典娜 AX6600 专属最新 iStoreOS 固件

on:
  workflow_dispatch:
    inputs:
      lan_ip:
        description: '默认 LAN 口 IP 地址'
        required: true
        default: '192.168.1.1'
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-22.04
    permissions:
      contents: write
    steps:
      - name: 1. 检出编译配置库
        uses: actions/checkout@v4

      - name: 2. 准备 Linux 编译依赖环境与释放磁盘空间
        env:
          DEBIAN_FRONTEND: noninteractive
        run: |
          sudo rm -rf /usr/share/dotnet /usr/local/lib/android /opt/ghc /opt/hostedtoolcache /usr/local/share/boost /etc/apt/sources.list.d/*
          sudo -E apt-get -qq update
          sudo -E apt-get -qq install -y build-essential clang flex bison gawk gettext git libncurses5-dev libssl-dev python3-distutils python3-pyelftools rsync unzip zlib1g-dev squashfs-tools device-tree-compiler swig python3-dev python3-setuptools
          sudo -E apt-get -qq clean

      - name: 3. 拉取高通 IPQ60xx (QSDK 12.5) 专属 OpenWrt 源码与 iStore 商店
        run: |
          git clone --depth 1 https://github.com/coolsnowwolf/lede.git openwrt || git clone --depth 1 https://github.com/immortalwrt/immortalwrt.git -b openwrt-21.02 openwrt
          cd openwrt
          echo "src-git istore https://github.com/linkease/istore-ui.git" >> feeds.conf.default
          echo "src-git linkease https://github.com/linkease/istore.git" >> feeds.conf.default
          ./scripts/feeds update -a
          ./scripts/feeds install -a

      - name: 4. 配置京东云雅典娜 AX6600 专属初始化配置与默认 IP
        run: |
          cd openwrt
          mkdir -p files/etc/uci-defaults/
          LAN_IP="${{ github.event.inputs.lan_ip }}"
          [ -z "$LAN_IP" ] && LAN_IP="192.168.1.1"
          
          echo "#!/bin/sh" > files/etc/uci-defaults/99-custom-ax6600
          echo "uci set network.lan.ipaddr='${LAN_IP}'" >> files/etc/uci-defaults/99-custom-ax6600
          echo "uci set system.@system[0].timezone='CST-8'" >> files/etc/uci-defaults/99-custom-ax6600
          echo "uci set system.@system[0].zonename='Asia/Shanghai'" >> files/etc/uci-defaults/99-custom-ax6600
          echo "uci commit network" >> files/etc/uci-defaults/99-custom-ax6600
          echo "uci commit system" >> files/etc/uci-defaults/99-custom-ax6600
          echo "exit 0" >> files/etc/uci-defaults/99-custom-ax6600
          chmod +x files/etc/uci-defaults/99-custom-ax6600

      - name: 5. 生成 AX6600 专属 ipq60xx 配置文件与下载依赖包
        run: |
          cd openwrt
          # 100% 匹配真机探针出的 ipq60xx/generic + jdcloud,ax6600
          echo "CONFIG_TARGET_ipq60xx=y" > .config
          echo "CONFIG_TARGET_ipq60xx_generic=y" >> .config
          echo "CONFIG_TARGET_ipq60xx_generic_DEVICE_jdcloud_ax6600=y" >> .config
          
          # 核心硬件与 Wi-Fi 6 QCN9074 NSS 驱动
          echo "CONFIG_PACKAGE_kmod-ath11k=y" >> .config
          echo "CONFIG_PACKAGE_kmod-ath11k-pci=y" >> .config
          echo "CONFIG_PACKAGE_ath11k-firmware-ipq6018=y" >> .config
          echo "CONFIG_PACKAGE_ath11k-firmware-qcn9074=y" >> .config
          echo "CONFIG_PACKAGE_kmod-usb3=y" >> .config
          echo "CONFIG_PACKAGE_kmod-usb-storage=y" >> .config
          echo "CONFIG_PACKAGE_kmod-usb-storage-uas=y" >> .config
          echo "CONFIG_PACKAGE_kmod-fs-ext4=y" >> .config
          echo "CONFIG_PACKAGE_kmod-fs-exfat=y" >> .config
          echo "CONFIG_PACKAGE_kmod-fs-ntfs3=y" >> .config
          
          # iStore 商店与核心 UI
          echo "CONFIG_PACKAGE_luci-app-store=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-quickstart=y" >> .config
          
          # 核心网络与应用组件
          echo "CONFIG_PACKAGE_dnsmasq-full=y" >> .config
          echo "CONFIG_PACKAGE_dnsmasq_full_dhcpv6=y" >> .config
          echo "CONFIG_PACKAGE_dnsmasq_full_ipset=y" >> .config
          echo "CONFIG_PACKAGE_dnsmasq_full_nftset=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-dockerman=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-samba4=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-ttyd=y" >> .config
          echo "CONFIG_PACKAGE_luci-app-diskman=y" >> .config
          
          make defconfig
          sed -i 's/CONFIG_PACKAGE_dnsmasq=y/# CONFIG_PACKAGE_dnsmasq is not set/' .config
          make download -j8
          find dl -size -1024c -exec rm -f {} \;

      - name: 6. 执行并行编译生成雅典娜 AX6600 固件
        run: |
          cd openwrt
          export FORCE_UNSAFE_CONFIGURE=1
          echo -e "$(nproc) thread compile"
          make -j$(nproc) || make -j1 V=s

      - name: 7. 归档整理雅典娜 AX6600 固件发布目录
        run: |
          mkdir -p bin_out
          find openwrt/bin/targets/ipq60xx/generic/ -type f \( -name "*jdcloud_ax6600*" -o -name "*factory*" -o -name "*.bin" -o -name "*.ubi" \) -exec cp -f {} bin_out/ \; 2>/dev/null || true
          touch bin_out/BUILD_COMPLETE.txt

      - name: 8. 上传京东云雅典娜 AX6600 Release 固件产物 (Artifacts)
        uses: actions/upload-artifact@v4
        with:
          name: iStoreOS-JDCloud-AX6600-Firmware
          path: bin_out/

      - name: 9. 自动发布固件到 GitHub Release 发行版
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v24.12-AX6600-${{ github.run_id }}
          name: 京东云雅典娜 AX6600 iStoreOS 最新固件 Release v24.12-${{ github.run_id }}
          body: |
            🎉 京东云无线宝 雅典娜 AX6600 (Qualcomm IPQ6018 + QCN9074) 专属 iStoreOS 固件自动编译发布完成！
            
            - 适配硬件：高通 IPQ6018 + QCN9074 (三频 Wi-Fi 6)
            - 刷机指引：在不死 U-Boot 中上传带 factory 关键字的固件刷入即可。
          files: bin_out/*
